// Exit codes used across the CLI and MCP server.
export const EXIT_SUCCESS = 0;
export const EXIT_UNEXPECTED = 1;
export const EXIT_CONFIG = 2; // usage / config errors
export const EXIT_PORT_IN_USE = 3;
